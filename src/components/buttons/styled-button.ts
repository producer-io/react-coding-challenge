import styled from 'styled-components';

export const Button = styled.button`
border-radius: .8rem;
padding: .6rem 1.2rem;
font-size: 1rem;
font-weight: 500;
font-family: inherit;

&:hover {
  cursor: pointer;
}
`